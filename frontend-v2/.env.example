# Firebase Auth (Google Sign-In)
# Create a Firebase Web App in the Firebase Console to get these values.
# These values are not secrets, but keep per-environment configs separate.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=

# Optional (not required for auth):
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=

# OCR API base for production/hosted builds:
VITE_API_BASE=
